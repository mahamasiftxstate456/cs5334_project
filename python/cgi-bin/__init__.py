# This file makes the cgi-bin/ directory a Python package.
# It must exist for Python to recognize cgi-bin/ as a package.
#
# This file intentionally has no code in it.
# Its presence alone is what matters to Python.